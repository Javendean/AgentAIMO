# Legacy Artifact Inventory

This document provides an inventory of all non-source-code artifacts currently residing in the repository. It classifies them using the strict phase-0 labels requested.

## 1. Generated Datasets & Logs (.jsonl)

| File | Status | Notes / Evidence |
| :--- | :--- | :--- |
| `research_data.jsonl` | requires manual review | **WARNING:** Referenced via sys.argv in `analysis/analyze_results.py` and hardcoded output paths in `kaggle_notebook.py`. Moving it requires updating pipeline documentation and Kaggle paths. |
| `research_data2.jsonl` | keep in place | Loaded directly via `open('research_data2.jsonl')` inside `verify_solved.py`. Moving it will break that script. |
| `research_data2.jsonl.bak` | move to _attic/noise later | Backup file. Not referenced by any code. |
| `dpo_correction_dataset.jsonl` | move to research/raw later | Generated output, not referenced by any local code. |
| `dpo_correction_dataset_v2.jsonl` | move to research/raw later | Generated output, not referenced by any local code. |
| `dpo_correction_dataset_v3.jsonl` | move to research/raw later | Generated output, not referenced by any local code. |
| `dpo_correction_dataset_v4.jsonl` | keep in place | **WARNING:** Referenced via `open('dpo_correction_dataset_v4.jsonl', 'w')` in `create_preference_dataset.py` (line 136). Moving it breaks the script's hardcoded write path. |

---

## 2. Transcripts, Chats, and Summaries (.txt, .md)

All files below were grepped for `open()` calls and internal python string references. None are referenced by the active Python codebase unless noted.

| File | Status | Notes / Evidence |
| :--- | :--- | :--- |
| `BrowserChat.txt` | move to _attic/noise later | Raw manual chat transcript. Unreferenced. |
| `GeminiChat.txt` | move to _attic/noise later | Raw manual chat transcript. Unreferenced. |
| `2511.01846v1_Part1.txt` | move to research/raw later | Text extract from arXiv paper. Unreferenced. |
| `pdf1.txt`, `pdf2.txt`, `pdf3.txt` | move to research/raw later | PDF text extracts. Unreferenced. |
| `pdf_abstracts.txt` | move to research/raw later | Compiled abstracts. Unreferenced. |
| `new_context_content.txt` | requires manual review | Unclear scratchpad context. Safest to review before archiving. |
| `c2_sorted_check.txt` | move to _attic/noise later | Debug terminal output dump. Unreferenced. |
| `prompt_patch.txt` | requires manual review | **WARNING:** This is a dynamic config file generated by `analysis/analyze_results.py` and loaded dynamically by `notebook/kaggle_notebook.py` on Kaggle. It must be managed carefully. |
| `research_summary.txt` | move to _attic/noise later | Manual summary file. Unreferenced. |
| `syntax_errors.txt` | move to _attic/noise later | Debug dump of errors. Unreferenced. |
| `annotation_research_context.md` | move to research/raw later | Conceptual notes. Unreferenced. |
| `mcts_rubric_analysis.md` | move to research/raw later | Conceptual notes. Unreferenced. |
| `Refining LLM Rubric for Human Reasoning.md` | move to research/raw later | Conceptual notes. Unreferenced. |
| `rubric.md` | move to research/raw later | Grading guidelines. Unreferenced. |
| `analysis/analysis_report.txt` | move to _attic/noise later | Terminal output dump residing in the analysis folder. Unreferenced. |

---

## 3. Jupyter Notebooks (.ipynb)

| File | `44-50-aimo3-skills-optional-luck-required.ipynb` |
| :--- | :--- |
| **Status:** | move to research/notebooks later |
| **Duplicates logic?** | **YES.** It defines an `AIMO3Sandbox`, an `AIMO3Solver`, and an `AIMO3Tool`. This is a complete structural duplication of the entire `agent/deep_researcher.py` and `agent/sandbox.py` logic stack. It appears to be an alternative, superseding, or highly customized fork of the agent architecture built specifically to interface with the `kaggle_evaluation.aimo_3_inference_server`. |
| **Unique logic?** | **YES.** It contains port management logic for the sandbox (`_get_next_ports`) and a unique interface `predict(id_, question, answer)` tailored for the official Kaggle evaluation API. |
| **Required Action:** | Because it duplicates core logic but adds critical, unique Kaggle evaluation hooks, this unique logic **must be extracted to a module** (e.g. `agent/kaggle_eval_harness.py`) before this massive notebook can be safely archived. |

---

## 4. Root-Level Python Scripts (Non-Package)

| File | Status | Notes / Evidence |
| :--- | :--- | :--- |
| `analyze_failures.py` | requires manual review | Overlaps heavily with `analysis/analyze_results.py`. Needs review to see if unique metrics exist. |
| `chunk_research_data.py` | requires manual review | Data processing script. |
| `clean_research_data.py` | requires manual review | Data processing script. |
| `create_preference_dataset.py` | requires manual review | Generates `dpo_correction_dataset_v4.jsonl` using hardcoded paths. |
| `create_submission_zip.py` | requires manual review | Deployment script. Likely assumes it runs from the repository root to zip the `agent/` folder. |
| `extract_syntax_errors.py` | move to _attic/noise later | Single-use regex debugging script over old logs. |
| `extract_top_leaderboard.py` | move to _attic/noise later | Extraneous web scraper script. |
| `filter_notebooks.py` | requires manual review | Utility script. |
| `fix_lines_16_91.py` | move to _attic/noise later | Highly specific string-replace script. Appears to be a one-off hack. |
| `inspect_zip.py` | requires manual review | Deployment debugging script. |
| `maximize_density.py` | move to research/raw later | Experimental context optimizer. |
| `optimize_context.py` | move to research/raw later | Experimental context optimizer. |
| `process_context_final.py` | requires manual review | Pipeline script. |
| `process_context_final_v2.py` | requires manual review | Iterated pipeline script. |
| `process_pdf_v2.py` | move to research/raw later | Utility for PDF text extraction. |
| `scrape_candidate_notebooks.py` | move to research/raw later | Playwright web scraper. |
| `split_dense_context.py` | requires manual review | Data formatting script. |
| `test_safeguard.py` | requires manual review | **UNCERTAIN:** This script imports `extract_code_blocks` and `run_verification` from `agent.sandbox`. It appears to be an informal scratchpad test. It is safer to review and move it into `tests/` rather than archive it. |
| `verify_context.py` | requires manual review | Data validation script. |
| `verify_solved.py` | requires manual review | Contains `open('research_data2.jsonl')`. Moving it breaks the hardcoded dependency. |