# Repository Map

This document provides a structural map of the repository as it currently exists, categorizing files by their apparent purpose and highlighting areas that require manual review or future cleanup.

## 1. Production-Like Code (Core Package)

The `agent/` directory contains the core logic for the DeepResearcher H100-Optimized Math Research Agent. This is the primary engine intended for execution during the Kaggle sprint.

*   **`agent/__init__.py`**: Package initialization.
*   **`agent/deep_researcher.py`**: The main controller. Handles the research loop, multi-sampling (majority voting), dynamic time allocation, Tool-Integrated Reasoning (TIR) orchestration, and fallback self-correction loops.
*   **`agent/prompts.py`**: Prompt templating engine. Contains the core `SYSTEM_PROMPT`, generation/verification templates, and heuristic logic for topic classification (`classify_topic`) and model family detection.
*   **`agent/sandbox.py`**: The sandboxed execution environment. Extracts python code blocks via regex and executes them using `subprocess` with a `SAFETY_PREAMBLE` to block dangerous modules.

## 2. Execution Harness (Mainline/Notebook Divergence)

The `notebook/` directory contains the script intended to be run in the actual Kaggle environment.

*   **`notebook/kaggle_notebook.py`**: The Kaggle execution script. It handles offline pip installs, initializes the `DeepResearcher`, and contains the highly rigid "Canary Test" logic used to trigger fallback models.
*   **Divergence Risk**: The Kaggle notebook acts as the true `main.py` but is structurally isolated from the `agent/` package. The "Canary Test" logic is hardcoded directly into this notebook rather than being a modular component of the agent, making it difficult to test in standard CI/CD pipelines.
*   **Note on `44-50-aimo3-skills-optional-luck-required.ipynb`**: Do not dismiss this root-level notebook as archival noise. It contains a functionally independent `AIMO3Solver` loop that bypasses the core package, representing meaningful package-vs-notebook divergence and a completely separate solver workflow.

## 3. Analysis and Grading Pipeline

The `analysis/` directory contains scripts for processing the artifacts generated by the agent.

*   **`analysis/analyze_results.py`**: Reads the `research_data.jsonl` output, aggregates success metrics, counts failure patterns (e.g., syntax errors, timeouts), and auto-generates a `prompt_patch.txt` file to feed back into subsequent runs.

## 4. Test Suite

The `tests/` directory contains unit tests for the core agent package.

*   **`tests/test_agent.py`**: Tests sandbox security blocklists, prompt formatting, extraction regexes, and performs dry runs of the core loop.
*   **`tests/test_hallucination_fix.py`**: A specific test verifying that the agent can recover from hallucinated tool-call formats (`assistantcommentary`).

## 5. Experimental / Research Code (Root Level)

The root directory is heavily cluttered with experimental scripts and one-off utilities used for data processing, context optimization, and dataset creation.

*   `analyze_failures.py`
*   `chunk_research_data.py`
*   `clean_research_data.py`
*   `create_preference_dataset.py`
*   `create_submission_zip.py`
*   `extract_syntax_errors.py`
*   `extract_top_leaderboard.py`
*   `filter_notebooks.py`
*   `fix_lines_16_91.py`
*   `inspect_zip.py`
*   `maximize_density.py`
*   `optimize_context.py`
*   `process_context_final.py`
*   `process_context_final_v2.py`
*   `process_pdf_v2.py`
*   `scrape_candidate_notebooks.py`
*   `split_dense_context.py`
*   `test_safeguard.py`
*   `verify_context.py`
*   `verify_solved.py`

## 6. Noisy / Archival Artifacts (Root Level)

The root directory contains a large number of generated datasets, raw text dumps, transcripts, and model outputs. These intermingle with the source code and create significant noise.

*   **Datasets (.jsonl)**: `dpo_correction_dataset.jsonl`, `dpo_correction_dataset_v2.jsonl`, `dpo_correction_dataset_v3.jsonl`, `dpo_correction_dataset_v4.jsonl`, `research_data.jsonl`, `research_data2.jsonl`, `research_data2.jsonl.bak`
*   **Raw Text/Dumps (.txt, .md)**: `2511.01846v1_Part1.txt`, `BrowserChat.txt`, `GeminiChat.txt`, `c2_sorted_check.txt`, `new_context_content.txt`, `pdf1.txt`, `pdf2.txt`, `pdf3.txt`, `pdf_abstracts.txt`, `prompt_patch.txt`, `research_summary.txt`, `syntax_errors.txt`, `annotation_research_context.md`, `mcts_rubric_analysis.md`, `Refining LLM Rubric for Human Reasoning.md`, `rubric.md`
*   **Notebooks (.ipynb)**: `44-50-aimo3-skills-optional-luck-required.ipynb`

## 7. Future Restructuring Candidates

*   **Root Cleanup**: The massive volume of root-level Python scripts (Section 5) and data artifacts (Section 6) should be aggressively moved into dedicated `scripts/`, `data/raw/`, `data/processed/`, and `_attic/` directories.
*   **Mainline Unification**: The Kaggle notebook logic (`notebook/kaggle_notebook.py`) should ideally be refactored into a standardized `main.py` entrypoint within the package, with Kaggle-specific environment setup abstracted away.